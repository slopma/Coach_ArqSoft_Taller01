# Nombre de la aplicación
spring.application.name=coach-system

# Configuración H2 Database (Base de datos en memoria)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sara
spring.datasource.password=1234

# Habilitar consola H2 (para ver la base de datos en el navegador)
spring.h2.console.enabled=true

# JPA/Hibernate configuración
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

# Thymeleaf configuración (para desarrollo)
spring.thymeleaf.cache=false